{
  "name": "daily-vocabulary-of-shiju-gu",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-ready PWA and cloud API for a personal vocabulary deck.",
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check public/app.js && node --check api/_server.js && node --check api/[...path].js"
  },
  "engines": {
    "node": ">=18"
  }
}
