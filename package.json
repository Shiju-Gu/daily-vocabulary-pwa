{
  "name": "daily-vocabulary-of-shiju-gu",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-ready PWA and cloud API for a personal vocabulary deck.",
  "scripts": {
    "check": "node --check app.js && node --check api/_server.js && node --check api/meaning.js && node --check api/audio.js && node --check api/pronunciation.js"
  },
  "engines": {
    "node": ">=18"
  }
}
