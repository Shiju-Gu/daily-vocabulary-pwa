{
  "name": "daily-vocabulary-of-shiju-gu",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-ready PWA and cloud API for a personal vocabulary deck.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check api/[...path].js"
  },
  "engines": {
    "node": ">=18"
  }
}
